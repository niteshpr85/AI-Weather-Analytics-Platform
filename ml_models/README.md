# ML Models

This folder is reserved for:
- Historical weather datasets
- Training notebooks/scripts
- Serialized model artifacts (`.pkl`, `.pt`, `.h5`)

Recommended structure:

```text
ml_models/
├── data/
├── notebooks/
├── training/
└── artifacts/
```

